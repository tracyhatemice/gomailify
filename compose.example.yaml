services:
  gomailify:
    image: ghcr.io/tracyhatemice/gomailify:latest
    build: .
    restart: unless-stopped
    environment:
      TZ: "UTC"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - gomailify-data:/app/data

volumes:
  gomailify-data:
